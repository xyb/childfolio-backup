# FIXME support windows with wincred

proc getPassword(serviceName, username: string): string =
  discard

proc setPassword(serviceName, username, password: string) =
  discard

proc deletePassword(serviceName, username: string) =
  discard
